# Limits - Key Concepts

## Standard Limits
lim(x→0) sin(x)/x = 1
lim(x→0) (e^x - 1)/x = 1
lim(x→0) (a^x - 1)/x = ln(a)
lim(x→∞) (1 + 1/x)^x = e

## Limit Laws
lim(f ± g) = lim f ± lim g
lim(f × g) = lim f × lim g
lim(f/g) = lim f / lim g (when lim g ≠ 0)

## Indeterminate Forms
0/0, ∞/∞, 0×∞, ∞-∞ require manipulation (L'Hôpital, factorization)

## L'Hôpital's Rule
If lim f(x)/g(x) gives 0/0 or ∞/∞, then lim f(x)/g(x) = lim f'(x)/g'(x)
